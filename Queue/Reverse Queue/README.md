##  Approach

1. Remove all elements from the queue and push them onto a stack  
2. Since stack follows LIFO order, elements get reversed  
3. Pop elements from the stack and push them back into the queue  
4. The queue now contains elements in reversed order
